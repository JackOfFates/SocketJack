namespace SocketJack.WpfBasicGame;

// Difficulty presets used to tune bot cursor speed and click aggressiveness.
// Bots also apply per-instance trait multipliers to add variation within the preset.
internal enum BotDifficulty {
    Easy,
    Normal,
    Hard
}
